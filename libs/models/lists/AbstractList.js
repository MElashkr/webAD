/**
 * definition of the abstract class "AbstractList",
 * extended by Generic, Static and Dynamic List classes
 */
class AbstractList {

    /**
     * constructs the AbstractList
     */
    constructor() {
    }

    /**
     * inserts element at head of the list
     * @param element to be inserted
     */
    add(element) {
    }

    /**
     * inserts element at specified position
     * @param element to be inserted
     * @param pos position to insert
     */
    addAtNthPosition(element, pos) {
    }

    /**
     * removes the first element of the list
     */
    removeFirstElement() {
    }

    /**
     * searches and removes a specific element
     * @param element to be deleted
     */
    removeElement(element) {
    }

    /**
     * removes element at specified position
     * @param pos position to be deleted
     */
    removeNthElement(pos) {
    }

    /**
     * returns the first element of the list
     * @returns {*} first element of the list
     */
    firstElement() {
    }

    /**
     * returns the element located at specified position
     * @param pos position
     * @returns {*} element at pos
     */
    accessNthElement(pos) {
    }

    /**
     * searches a specific element
     * @param element to be searched
     */
    searchElement(element) {
    }

    /**
     * counts the existing elements
     */
    countElements() {
    }

    /**
     * draws the view
     */
    draw() {
    }

    /**
     * zooms in
     */
    zoomIn() {
    }

    /**
     * zooms out
     */
    zoomOut() {
    }

    /**
     * makes a copy of the list
     */
    copy() {
    }
}